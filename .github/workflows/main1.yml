name: print
on: push
env:
  continue: true
  time: 3
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - continue-on-error: "${{ env.continue }}"
        timeout-minutes: "${{ env.time }}"
        run: echo ...
