name: testing-conditionals

on:
  workflow_dispatch

env:
  myvalue: ${{ github.ref == 'refs/heads/dotnet_github' && 'workingdir' || 'testbranch' }}

jobs:
  job:
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: |
          echo ${{ env.myvalue }}
