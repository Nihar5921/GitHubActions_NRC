name: github_output

on:
 workflow_dispatch

jobs:
 job1:
   runs-on: ubuntu-latest
   outputs:
       value: ${{ steps.step1.outputs.value }}
   steps:
      - name: Generate_output
        id: step1
        run: echo "value=dummy" >> GITHUB_OUTPUT

 job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: Display
        run: echo ${{ needs.job1.outputs.value }}

         
     
    
